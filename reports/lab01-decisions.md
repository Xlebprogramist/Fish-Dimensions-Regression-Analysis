# Журнал решений — лабораторная работа №1

| # | Шаг | Решение | Альтернативы | Обоснование | Проверка |
|---|---|---|---|---|---|
| 1 | 3.1 | Python 3.7 → 3.13 в `Pipfile` | brew install python@3.7, docker | Python 3.7 EOL 2023-06-27; под 3.13 есть готовые wheels | `pipenv sync --dev` — успешно |
| 2 | 3.1 | Удалить `light=False` в `dbc.NavbarSimple` | Откат `dash-bootstrap-components` до `<2.0` | Свежая библиотека предпочтительнее; одна строка правки | `run.py` запускается |
| 3 | 3.1 | `app.run_server` → `app.run` | Откат Dash до `<3.0` | Dash 4.x — текущий API; изменение одной строки | Приложение открывается в браузере |
| 4 | 3.1 | Сохранить исходные ошибки в `reports/*.txt` | Проигнорировать | Требование задания: фиксировать исходную ошибку до минимальных правок | Файлы в репозитории |
| 5 | 3.1 | Атомарные коммиты через `git rebase -i` | Один большой коммит | Требование критериев: не менее 4 содержательных коммитов | 5+ коммитов в PR |
| 6 | 3.1 | Минимум 2 теста в `tests/` | Без тестов | Требование общей инструкции, раздел 4.1 | `pytest tests/ -v` — 6 passed |
| 7 | 3.7 | Разбиение train/val/test | — | Будет определено в шаге 3.7 | — |

## Удаление аномальной строки (Weight = 0)

- **Что:** строка `Roach` с `Weight = 0.0` (исходный индекс 40) удалена из `assets/data/Fish.csv`.
- **Почему:** физически невозможно — рыба не может весить ноль при нормальных геометрических размерах (Length1=19, Length2=20.5, Length3=22.8, Height=6.48, Width=3.35).
- **Тип аномалии:** ошибка измерения или ввода, не выброс. Отличается от «крупная рыба» — там значение правдоподобно.
- **Альтернативы:**
  - оставить с флагом `is_weight_anomaly` — отклонено, одна строка не оправдывает новый столбец;
  - восстановить вес по регрессии длина→масса — отклонено, добавляет синтетику;
  - оставить как есть — отклонено, портит целевую переменную.
- **Проверка:** `assets/data/Fish.csv` содержит 158 строк данных, `min(Weight) > 0`.
- **Влияние:** одна строка из 159 (<0.7%), распределение целевой переменной не меняется значимо.

## Шаг 3.3 — результаты автоматических проверок

Модуль: `src/data-checks.py`. Запуск: `pipenv run python src/data-checks.py`.

| Проверка | Результат |
|---|---|
| shape | 158 × 7 |
| Полные дубликаты | 0 |
| Пропуски | 0 |
| Бесконечности | 0 |
| Геометрия > 0 | да, все столбцы |
| Length1 ≤ Length2 ≤ Length3 | да |
| Height < Length3, Width < Length3 | да |
| Редкие категории (порог 5) | нет (Whitefish = 6) |
| Дисбаланс видов | 9.33× (Perch 56 / Whitefish 6) |
| Почти дубликаты | 0 |
| Столбцы-идентификаторы | нет |

**Решения:**
- Дисбаланс 9.33× — повод для **стратифицированного разбиения** (шаг 3.7).
- Столбцы с высокой уникальностью (Height 0.968, Width 0.956) — не ID,
  но проверить при построении производных признаков (шаг 3.6).
- Аномалий уровня Weight=0 больше нет.
